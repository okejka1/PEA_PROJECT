//
// Created by User on 13.10.2023.
//

#include "Menu.h"
#include "iostream"

using namespace std;
/*
void Menu::show(){
    int option = -1;
    do {
        std::cout << "\n==== TSP MENU ====\n";
        std::cout << "1.Brute Force algorithm\n";
        std::cout << "2.B&B algorithm\n";
        std::cout << "3.Dynamic Programming algorithm\n";
        std::cout << "4.Exit\n";

        std::cin.clear();
        fflush(stdin);

        std::cin >> option;


        switch (option) {

            case 1: // loading from a file

                break;
            case 2: // generating a random graph
                std::cout << "Enter number of vertices and density of the graph\n";
                std::cin >> num >> den;
                mst.generate_random_graph(num, den);
                mst.printMatrix();
                mst.printList();
                break;
            case 3: // display of matrix and list
                mst.printMatrix();
                mst.printList();
                break;
            case 4: // Prim's algorithm
                mst.primList();
                mst.primMatrix();
                mst.printMatrix();
                mst.printList();
                break;
            case 5: //
                mst.measureTime(50);
                break;
            case 6: // exit
                break;
            default:
                std::cout << "Input error!\n";
                break;
        }
    } while (option != 6);
}



}
 */